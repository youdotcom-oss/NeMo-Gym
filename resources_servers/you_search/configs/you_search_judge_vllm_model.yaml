you_search_resources_server:
  resources_servers:
    you_search:
      entrypoint: app.py
      domain: agent
      debug: false
      ydc_api_key: ${ydc_api_key}
      # snippets | highlights | full_page | eco — see resources_servers/you_search/README.md
      search_mode: snippets
      use_judge: true
      judge_model_server:
        type: responses_api_models
        name: judge_model
      judge_responses_create_params:
        input: []
      verified: false
      description: Model uses search tools to satisfy a user query.
      value: Measure agentic search capability
judge_model:
  responses_api_models:
    vllm_model:
      entrypoint: app.py
      base_url: ${search_judge_model_base_url}
      api_key: ${search_judge_model_api_key}
      model: ${search_judge_model_name}
      return_token_id_information: false
      uses_reasoning_parser: true
you_search_simple_agent:
  responses_api_agents:
    simple_agent:
      entrypoint: app.py
      resources_server:
        type: resources_servers
        name: you_search_resources_server
      model_server:
        type: responses_api_models
        name: policy_model
      datasets:
      # Task rows carry only question / ground_truth / tool schema — nothing
      # provider-specific — so they are shared verbatim with tavily_search. Running
      # both servers over the same rows is what makes the arms comparable.
      - name: example
        type: example
        jsonl_fpath: resources_servers/you_search/data/example.jsonl
        num_repeats: 1
      - name: benchmark_browsecomp
        type: train
        jsonl_fpath: resources_servers/you_search/benchmark/browsecomp/browsecomp_test_set.jsonl
        num_repeats: 1
        gitlab_identifier:
          dataset_name: tavily_search
          version: 0.0.1
          artifact_fpath: browsecomp_test_set.jsonl
        license: Apache 2.0
      - name: simpleqa_benchmark
        type: train
        jsonl_fpath: resources_servers/you_search/benchmark/simpleqa/simpleqa_test_set.jsonl
        num_repeats: 1
        gitlab_identifier:
          dataset_name: tavily_search
          version: 0.0.1
          artifact_fpath: simple_qa_test_set.jsonl
        license: Apache 2.0
